const routes = {
  init: '/', // tenuta con copertina
  home: '/home', // tenuta
  famiglia: '/famiglia',
  prodotti: '/prodotti',
  territorio: '/territorio',
  // event: (id = ':id') => `/events/${id}`,
  // newEvent: '/new-event',
};

export default routes;
